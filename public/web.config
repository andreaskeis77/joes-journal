<?xml version="1.0" encoding="UTF-8"?>
<!--
  Static-hosting config for the IIS site that serves Astro's dist/.
  Lives in public/ so Astro copies it to dist/web.config on every build.

  Purpose: ensure IIS serves modern static asset types with correct MIME
  types. Without an explicit .webp mapping, IIS returns 404 for .webp files
  (broken hero/cover images). <remove> precedes each <mimeMap> so this never
  throws "duplicate collection entry" if the type is already defined at the
  server level.
-->
<configuration>
  <system.webServer>
    <staticContent>
      <remove fileExtension=".webp" />
      <mimeMap fileExtension=".webp" mimeType="image/webp" />
      <remove fileExtension=".svg" />
      <mimeMap fileExtension=".svg" mimeType="image/svg+xml" />
      <remove fileExtension=".woff2" />
      <mimeMap fileExtension=".woff2" mimeType="font/woff2" />
      <remove fileExtension=".webmanifest" />
      <mimeMap fileExtension=".webmanifest" mimeType="application/manifest+json" />
    </staticContent>
  </system.webServer>
</configuration>
